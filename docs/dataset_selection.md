# Dataset Selection — vetting rubric & decision log

**Purpose.** The *rules and rationale* for choosing training data, plus a running
**accept / reject / pending** log so decisions stay consistent and rejected datasets
aren't re-evaluated. The specific-dataset registry lives in
[`clean_sft_datasets.md`](clean_sft_datasets.md); what's *currently used* is in
[`datasets.md`](datasets.md); license consequences are in the README "Licensing &
data provenance" section. **This file is the "how we decide."**

> **The one lesson behind all of it (2026-06-20):** *verify the generating model,
> not the source.* MIRIAD looked clean ("grounded in S2ORC") but was **generated by
> OpenAI's models** — disqualifying. Removing OpenHermes was necessary but not
> sufficient. **Read the card + paper; assume nothing.**

---

## Hard constraints (gates — a dataset must pass ALL of these to be eligible)

1. **No proprietary-model-output provenance — *any* vendor.** No *training data*
   generated by or sourced from a closed/proprietary model: **OpenAI (GPT), Anthropic
   (Claude), Google (Gemini), xAI (Grok)** — all of them — incl. user↔model logs
   (WildChat/ShareGPT). They all restrict using outputs to train competing models →
   gates distribution. **Check the *generator*, not just the input/grounding.** Clean
   generators = **open models with permissive output-reuse** (Qwen, Mistral — Apache;
   Llama 3.1+).
   - **Scope: training data only.** Using these models as **research / coding
     assistants** (hardware advice, finding papers, debugging, docs) does *not* trigger
     this — the gate is about what goes *into the training set*, not what informs
     decisions. The bridge to avoid: turning a proprietary model's *output* into a
     *training example*.
2. **Redistributable license** (for an open/distributable dataset): CC-BY, CC0,
   Apache-2.0, MIT, ODC-By. **Avoid** CC-BY-**NC** (non-commercial) if commercial-clean
   is wanted, **ND** (no-derivatives — can't even make a derived dataset), and
   **credential-gated** clinical data (MIMIC-III/IV, i2b2, eICU — DUA-bound, **cannot
   be redistributed**).
3. **Generator-license clean** (if model-generated): the *generating* model's license
   must permit using its outputs to train other models — **Apache models (Qwen,
   Mistral) and Llama 3.1+ are OK**; avoid generators that restrict output reuse.
4. **No benchmark contamination** with our eval sets (GSM8K, ARC, etc.) — run
   `data/contamination.py`; drop overlapping samples.

---

## ✅ Green flags (what to look for)

- **Human-authored** (no model generation) — cleanest possible provenance.
- **Generated by a *disclosed open* model** (Llama 3.1+/Qwen/Mistral), generator named.
- **Verification loops** — code with unit-test execution status; math with checked answers.
- **Grounded generation** (RAG-style: generated *from* a cited source passage) — factual,
  citable, low-hallucination. Keep the citation with each item.
- **Permissive, clearly-stated license** (CC-BY/CC0/Apache/MIT/ODC-By), ideally per-item.
- **Documented provenance** — the card/paper names the generator and the source.

## ❌ Red flags (what to avoid / treat as suspect)

- **"Generated with GPT-4 / GPT-4o / ChatGPT / OpenAI"** anywhere in the pipeline → reject.
- **Undisclosed generator** — "synthetic"/"hybrid" with no model named → **suspect until
  verified** (don't assume open). Read the linked paper.
- **GPT-distilled instruction sets** — Alpaca, many "*-Evol-Instruct"/"*-Instruct" sets,
  OpenHermes, ShareGPT, WildChat-GPT — all OpenAI-output.
- **Misleading wording:** "no proprietary model *knowledge*" (describes grounding, not the
  generator — MIRIAD's exact dodge); "open dataset" (license ≠ provenance);
  "research-friendly" (often = NC or murky).
- **NC / ND licenses** if distributing; **credential-gated** clinical data (can't redistribute).
- **Benchmark overlap** with our evals (contamination).

---

## Vetting procedure (run for every candidate)

1. **Find the generating model** — read the HF card *and* the linked paper. If it's not
   stated, it's **pending**, not clean.
2. **Check the license** — per-item if the set is mixed; confirm it's in the
   redistributable set above.
3. **Check generator license** (if model-made) — outputs reusable for training?
4. **Contamination scan** vs. our eval benchmarks.
5. **Record the decision below** (accept / reject / pending + the reason).

---

## Decision log

| Dataset | Decision | Reason |
|---|---|---|
| OpenHermes 2.5 | ❌ reject | OpenAI-output provenance |
| Magicoder-Evol-Instruct | ❌ reject | OpenAI-output (Evol-Instruct via GPT) |
| MetaMathQA | ❌ reject | augmented via GPT-3.5 (OpenAI provenance) |
| **MIRIAD** | ❌ reject | QA **generated by OpenAI's models** (S2ORC was only grounding); + OpenAI-ToS ban on medical-diagnosis use |
| Tulu-3 SFT mixture (wholesale) | ❌ reject as-is | contains WildChat-GPT-4 + GPT-4o persona + CC-BY-NC subsets |
| Tulu-3 (filtered subsets) | ⚠️ conditional | usable IF filtered to human/open-model/permissive subsets only |
| NuminaMath-CoT | ⚠️ pending | generator undisclosed on card; broader project used GPT-4o — verify |
| OpenCodeInstruct | ⚠️ pending | generator undisclosed (no OpenAI mention; NVIDIA NeMo-Skills, CC-BY-4.0) — verify via arXiv 2504.04030 |
| ChemData700K | ⚠️ pending | construction undisclosed (no OpenAI mention; MIT) — verify via arXiv 2402.06852 |
| OpenAssistant `oasst2` | ✅ accept | human-written (Apache 2.0) |
| PubMedQA `pqa_artificial` | ✅ accept | rule-based auto-gen, pre-GPT (MIT) |
| OpenMathInstruct-2 | ✅ accept | Llama-3.1-405B (open, not OpenAI; Apache); keep contamination guard |
| FineWeb-Edu / OpenWebMath / CodeParrot-clean | ✅ accept (distill) | open web/code corpora (ODC-By / per-file); current distill mix |
| MedQA (USMLE) / MedMCQA | 🔎 considering | human-authored medical exam Q&A — verify exact licenses at ingestion |
| Self-built from **PMC OA `oa_comm`** | 🔎 considering | the clean medical path — see below |

---

## Building our own (the clean medical path)

Since MIRIAD is out and medical is the core domain, the most defensible source is
**self-built from the PMC Open Access Subset:**
- Pull the **`oa_comm`** bucket (commercial-use OK: CC-BY/CC0) via NCBI FTP
  (`ftp.ncbi.nlm.nih.gov/pub/pmc/`) or the OA Web Service — **not** the web reader
  (free-to-read ≠ redistributable). NCBI pre-sorts by license, so `oa_comm` is the
  clean bucket.
- Parse the **JATS XML** (`pubmed_parser` / lxml) into passages; keep **license +
  citation** per article.
- **Grounded generation** with an **open model** (Qwen/Mistral/Llama-3.1+) — generate QA
  *from the passage*, not from the model's memory. Verify + dedup + contamination-scan.
- **Dual-use:** the same `oa_comm` corpus is also the future MemPalace/RAG retrieval store.

---

*Update this file whenever a dataset is evaluated — the registry holds the specifics,
this holds the rules and the verdicts.*


<!-- ===== moved from docs/roadmap.md (2026-06-27 doc reorg) ===== -->

## Data roadmap

What data feeds which stage. The SFT stage is built; the rest are planned and
listed here so the data decision is made before the engineering, not during.

| Stage | Status | Datasets | Format / notes |
|-------|--------|----------|----------------|
| **Distillation** | ✓ done | FineWeb-Edu (40%) · open-web-math (40%) · codeparrot-clean (20%) | Raw text + teacher logits. Math-heavy to transfer Ouro-Thinking's reasoning. |
| **SFT** | ✓ done (v2–v4) | OpenHermes-2.5 (30%) · MetaMathQA (40%) · Magicoder-Evol-Instruct (30%) | ChatML, loss masked to response tokens. Use `seq_len≥1024` for OpenHermes (multi-turn) — at 512 it's ~95% rejected. |
| **Preference tuning (DPO/ORPO)** | 📋 planned | `HuggingFaceH4/ultrafeedback_binarized`, `Anthropic/hh-rlhf` | Needs `(prompt, chosen, rejected)` triples. ORPO folds it into one stage (no separate reward model) — simplest for a solo overnight setup. Start small (~10k pairs at this scale). |
| **Reasoning RL (GRPO)** | 📋 planned | GSM8K + MATH (train splits) with a **programmatic verifier** as the reward | The reward is a correctness checker (parse final answer, compare), not a learned reward model. This is where the recurrent-depth architecture should shine — more loops on harder problems. Memory-heavy (multiple rollouts/step); needs the 8-bit-Adam + small-batch budget. |
| **Tool use / function calling** | 📋 later | `glaiveai/glaive-function-calling-v2`, ToolBench subsets | SFT-style, but responses contain structured tool-call tokens. Define the call schema first. |
| **Long-context** | 📋 later | Curated long documents (books, repos) | Only meaningful once a model is coherent; the Ouro tokenizer supports 131k but nothing's been trained past 1k. |

### Planned domain expansion: science + medical (user goal, 2026-06-10)

**Clean-provenance candidates now live in [docs/clean_sft_datasets.md](clean_sft_datasets.md)** (MIRIAD, PubMedQA, ChemData, OpenWebMath-SFT — all OpenAI-free, superseding the GPT-provenance-flagged sets below where they overlap). Add science/medical as a data *kind* across the stages — per the variety
principle below, a new domain is exactly the kind of addition that has moved
behaviour at this scale. Candidates by stage (verify licenses at use time):

| Stage | Candidate datasets | Notes |
|-------|--------------------|-------|
| **Distillation / pretrain mix** | `allenai/peS2o` (open academic papers, clean license) · RedPajama **arXiv** subset · **PMC Open Access** (biomedical full text) · PubMed abstracts | Slot into `MixedDataset` as new sources with their own mix ratios — config work, no new code. For the 3B from-scratch run, include from day one rather than retrofitting. |
| **SFT** | `SciQ` (science QA) · `MedMCQA`, `PubMedQA`, `MedQA-USMLE` (medical QA → instruction format) · CAMEL science dialogues | Same ChatML / loss-mask pipeline as OpenHermes. **Provenance check per set** — several popular medical-chat sets (ChatDoctor, Medical Meadow variants) are GPT-derived → same OpenAI-ToS flag as OpenHermes (fine for research, a constraint if distributing). |
| **Eval** | MedQA / PubMedQA / SciQ accuracy | Add to `eval/metrics.py` alongside ARC — same cloze/log-likelihood pattern, small lift. |

**Safety note (medical specifically):** at proof-of-concept scale this is a
*domain-data experiment*, not a medical model — outputs are research artifacts,
never advice. If the domain survives to a coherent-scale model, revisit the
parked "safety alignment" roadmap item *before* anything medical-flavoured is
distributed or served.

**General principle observed so far:** at this scale, *data variety* moved
behaviour more than *data volume* or *parameter count*. Adding OpenHermes (v4)
unlocked the social-prompt register and recovered the confidence-halt that MoE
growth had blurred — a data change, not a scale change. Prefer adding a new
*kind* of data over more of the same.

---

